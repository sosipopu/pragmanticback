export type CreateUserParams = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};

export type UpdateUserParams = {
  firstName: string;
  lastName: string;
  email: string;
  password: string;
};
